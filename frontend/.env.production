# Production environment
# Use /api to leverage nginx proxy configuration
# Nginx will proxy /api requests to http://localhost:5000
VITE_API_URL=/api
VITE_APP_NAME=Fashion Store
VITE_ENABLE_ANALYTICS=true